// create a function largerThan that will receive 2 numbers as parameters and return the largest number only

const largerThan = (x, y) => {
    return Math.max(x, y);
};


console.log(largerThan(100, 130))